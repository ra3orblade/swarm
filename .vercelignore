apps/desktop/src-tauri/target
node_modules
# the built package; package.json stays because the site build reads its name
npm/*
!npm/package.json
.git
*.db
npm-team
# worktrees live here (.claude/worktrees/<name>); each is a full checkout with its own build output
.claude
