# Optional per-repo config. Everything here has a default; delete any line you don't need.
name = "example"
tasks = "docs/plan.md"          # markdown-table task source (M1.6)
worktrees = "~/.swarm/worktrees/example"

[resources]
web = { ports = "3400-3499" }
worker = {}
db = { ports = "54320" }

[rules]
shared-tree-readonly = true
no-pattern-kill = true
claim-required-to-write = false
